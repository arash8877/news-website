import {
    LAST_NEWS_REQUEST,
    LAST_NEWS_SUCCESS,
    LAST_NEWS_FAIL,
  } from "../constants/lastNewsConstants";
  
  export const lastNewsReducer = (state = { lastPosts: []}, action ) => {

  };